#Vamos a empezar con la clase de hoy INGRESE FECHA
nombre = input("ingrese su nombre: ")

#ahora empezamos a escribir la primera parte del juego de penales
import random

arquero = random.randint(1, 3)  # 1=izquierda, 2=centro, 3=derecha
print("el arquero eligio un lado (todavia no lo mostramos)")

jugador = int(input("elegi donde patear (1=izquierda, 2=centro, 3=derecha): "))

#por ahora solo mostramos los dos numeros, para revisar que funciona
print("arquero:", arquero)
print("jugador:", jugador)
